export default { command: ['e', 'exec', 'execute'], async run(m, { conn, args = '', isOwner }) { if (!isOwner) { return m.reply('*Este comando solo puede ser utilizado por el creador* ✿') } const code = Array.isArray(args) ? args.join(' ') : String(args) if (!code.trim()) { return m.reply('*Ejecuta una secuencia en JavaScript* ❀') } try { const AsyncFunction = Object.getPrototypeOf( async function () {} ).constructor const execute = new AsyncFunction( 'm', 'conn', 'args', 'process', 'require', code ) const result = await execute( m, conn, args, process, typeof require !== 'undefined' ? require : undefined ) if (result !== undefined && result !== null) { await m.reply(String(result)) } } catch (error) { await m.reply( `Error al ejecutar JavaScript:\n\n${error?.stack || error}` ) } } }
